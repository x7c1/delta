//! Read-side listing use cases: sessions, threads, and a thread's messages.

mod list_sessions_page;
mod open_sends_for;
mod require_thread;
mod thread_view;
mod threads_for;

#[cfg(test)]
mod tests;
