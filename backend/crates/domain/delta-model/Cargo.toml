[package]
name = "delta-model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# `serde_json::Value` is used as plain data for opaque tool payloads
# (`ContentBlock::ToolUse.input` / `ToolResult.content`). No serde derives:
# serialization lives in the gateway crates' wire/record twins.
serde_json = { workspace = true }
