{
  "//": "Resolution-only tsconfig used by dependency-cruiser. The `paths` map each @delta/* package to its TypeScript source so workspace edges resolve to src (not the excluded dist), letting the layer rules be enforced. Not used by builds.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@delta/model": ["packages/domain/model/src/index.ts"],
      "@delta/ui-kit": ["packages/ui/ui-kit/src/index.ts"],
      "@delta/api-client": ["packages/gateway/api-client/src/index.ts"],
      "@delta/api-mocks": ["packages/testing/api-mocks/src/index.ts"]
    }
  }
}
