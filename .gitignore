# Node / frontend
node_modules/
dist/
dist-types/
.vite/
*.tsbuildinfo

# Playwright (end-to-end) artifacts
test-results/
playwright-report/

# Rust / backend
/backend/target/
# Local SQLite database created by a dev run.
*.db
*.db-shm
*.db-wal
# Pre-migration snapshots the schema ladder writes before a destructive step.
# Never deleted automatically — keep them out of the tree, not off the disk.
*.db.bak-v*

# Scratch / agent progress logs
.tmp/

# Local env
.env
.env.local

# Host-specific make overrides (see local.mk.example)
local.mk

# OS / editor
.DS_Store
